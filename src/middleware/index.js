import { Router } from 'express';

export default ({ config, db }) => {
  let routes = Router();

  /**
   * Add middleware routes here
   * Anything that needs to be used as a stack
   * Can be used consistently
   */

  return routes;
}


